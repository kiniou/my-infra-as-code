# -*- mode: yaml -*-

file_client: local
file_roots:
  base:
    - ../states

ext_pillar_first: True
ext_pillar:
  - stack: ../pillar.stack/top.cfg
  - stack: ../pillar.stack/bottom.cfg

top_file_merging_strategy: same

default_top: base

pillar_merge_lists: false
